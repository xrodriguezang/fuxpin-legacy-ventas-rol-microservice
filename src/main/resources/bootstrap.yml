spring:
  profiles:
    # Profile actived. example: ventas-localhost.yml. I'ts default value
    active: localhost
  cloud:
    config:
      # user + password Config Cloud Config Server
      username: ${rest.user}
      password: ${rest.password}
      # DEVELOPMENT!
      # uri: https://xrodrig.dnsnet.info:8445/fuxpin-config-server/
      uri: https://pi.intranet.cat:8446/fuxpin-config-server/
      name: fuxpin-role-legacy-microservice
      # branch: master / develop / feature ...
      label: develop
  config:
    import: configserver:https://pi.intranet.cat:8446/fuxpin-config-server/